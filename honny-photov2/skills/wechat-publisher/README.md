# WeChat Publisher

> 为 Claude Code 打造的微信公众号发布助手

将 Markdown 文章智能排版并一键发布到微信公众号草稿箱。AI 生成创意 HTML，自动上传图片，零人工干预。

## ✨ 特性

- 🎨 **AI 驱动排版** - 非模板化，每次生成独特的创意 HTML 样式
- 🖼️ **自动图片处理** - 智能上传本地图片到微信 CDN
- 🎯 **零配置使用** - 安装后直接说"发布到微信"即可
- 🛡️ **智能修复** - 自动处理微信编辑器的兼容性问题
- 📦 **开箱即用** - 内置默认封面，无需手动准备

## 📦 安装

### 通过 Skills.sh 安装（推荐）

```bash
npx skills add artshooter/wechat-publisher
```

### 手动安装

1. 克隆仓库：
```bash
git clone https://github.com/artshooter/wechat-publisher.git
cd wechat-publisher
```

2. 复制到 Claude Code skills 目录：
```bash
cp -r . ~/.claude/skills/wechat-publisher/
```

## ⚙️ 配置

首次使用需要配置微信公众号凭证：

### 1. 获取微信公众号凭证

1. 登录 [微信公众平台](https://mp.weixin.qq.com)
2. 设置与开发 → 基本配置
3. 复制 **AppID** 和 **AppSecret**

### 2. 创建配置文件

在 skill 目录下创建 `config.json`：

```bash
cd ~/.claude/skills/wechat-publisher/
cp config.json.example config.json
```

编辑 `config.json`，填入你的凭证：

```json
{
  "appid": "wx1234567890abcdef",
  "appsecret": "你的AppSecret"
}
```

### 3. 配置 IP 白名单

如果遇到 `40164` 错误（IP 地址不在白名单），需要：

1. 登录微信公众平台
2. 设置与开发 → 基本配置 → IP 白名单
3. 添加你的服务器 IP（可以使用 `curl ifconfig.me` 查看）

## 🚀 使用

在 Claude Code 中，只需告诉 Claude：

```
发布到微信
```

或者：

```
把这篇文章推送到公众号
```

Claude 会自动：

1. 读取你的 Markdown 文章
2. 使用 AI 生成符合微信规范的精美 HTML
3. 上传所有本地图片到微信 CDN
4. 自动添加默认封面（如果文章没有图片）
5. 发布到微信公众号草稿箱

## 📁 项目结构

```
wechat-publisher/
├── SKILL.md                      # Claude Code Skill 定义
├── README.md                     # 项目文档
├── LICENSE                       # MIT 许可证
├── config.json.example           # 配置文件示例
├── assets/
│   └── default_cover.png         # 默认封面图（900×500）
├── references/
│   └── AI_CONVERTER_PROMPT.md    # AI 转换提示词
└── scripts/
    ├── publisher.py              # 微信发布核心逻辑
    └── image_uploader.py         # 图片上传工具
```

## 🔧 高级用法

### 命令行使用

也可以直接通过命令行使用发布工具：

```bash
python3 ~/.claude/skills/wechat-publisher/scripts/publisher.py \
  --title "文章标题" \
  --content article.html \
  --cover cover.png \
  --author "作者名"
```

### 仅上传图片

```bash
python3 ~/.claude/skills/wechat-publisher/scripts/image_uploader.py image.png
```

## 🤝 贡献

欢迎提交 Issue 和 Pull Request！

## 📄 许可证

[MIT License](LICENSE)

## 🙏 致谢

- 基于 [Skills.sh](https://skills.sh/) 生态构建
- 由 [Claude Code](https://claude.ai/code) 驱动

---

**提示**：本项目仅用于个人学习和合法的内容发布。请遵守微信公众平台运营规范。
